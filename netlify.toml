[build]
  base    = "frontend"
  command = "npm run build"
  publish = "dist"

[build.environment]
  NETLIFY_SKIP_PYTHON_SETUP = "true"
  NODE_VERSION = "20"

[[redirects]]
  from   = "/*"
  to     = "/index.html"
  status = 200
